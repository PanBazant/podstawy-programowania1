#include <iostream>
#include <string>
using namespace std;

int main() {
   /* const char x[]= "ala ma kota";
    int size=sizeof(x)/sizeof(x[0]);
    for (const char* i=x; i<x+size;i++)
        cout<< *i;
   string s("ala ma kota");
   cout<<endl;
    for(int i=0; i<s.size();i++){
        cout << s.operator[](i) << "!";
    }
    cout<<endl;

    for(string::iterator it=s.begin();it!=s.end();it++){
        cout << *it << "_";
    }
    */
   /* 1 zadanie string s;
    cin>>s;
    char min='z', max='a';
    for(string::iterator it=s.begin(); it!=s.end();it++){
        if(*it>max) max=*it;
        if(*it<min) min=*it;
    }

    cout<< min <<" "<< max;

    for(char c : s) {
        if('a'<= c && c<= 'z'){
        if(c>max) max=c;
        if(c<min) min=c;
        }
    } */
    
    /* 2
     string edited("ala ma kota i psa");
     string removed("kota");
     string added="prosiaka;
     size_t pos=edited.find(removed);
     if(pos!=string::npos)
        edited.replace(pos,removed.length(), added);
        cout<< edited<<endl;
    v2
    
    3 
    ofstream os;
    os.open("/tmp/file.txt");
    os<<"Hello world"<<endl;
    os.close;
   ifstream is;
   string s;
while(is!=eof())  
std::getline(is,s);
   cout<<s;
    
   */
   
   
   
   
    
 
    
    
    /*string napis1,napis2,napis3;
    cout<<"Podaj pare wyrazow\n";
    cin>>napis1;
    cout<<"Podaj napis2\n";
    cin>>napis2;
    cout<<"Podaj napis3\n";
    cin>>napis3;
    
    size_t x=napis1.find(napis2);
    napis1.replace(x,napis2.size,) */
    /*fstream x;
    string sciezka;
    cout << "Podaj sciezke do pliku"<<endl;
    x.open(sciezka,ios::out)
    string linia;
    while(!plik.eof()){
        getline(x,linia)
                if(linia.size()<=15)
    }*/
    string napis;
    cin>>napis;
    ofstream file;
    file.open("/tmp/text.txt");
    
    string::size_type last
